
1. Initialize, and push initial setup, with jenkinsfile or dockerfile

2. Create branches
   git checkout -b preprod
   git push origin preprod
   git checkout -b develop
   git push origin develop

   git add Jenkinsfile commands.txt text_main.py
   git commit -m "Add Jenkinsfile, ..."
   git push origin develop